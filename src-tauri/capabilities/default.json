{
  "$schema": "../gen/schemas/desktop-schema.json",
  "description": "Base capability shared by the main and preference windows: core window/event/app APIs only. Plugin- and feature-specific grants are added per phase (window mgmt, paste, shortcuts, settings).",
  "identifier": "default",
  "permissions": [
    "core:default",
    "core:window:allow-set-theme",
    "log:default",
    "os:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "macos-permissions:default",
    "opener:default",
    "core:window:deny-internal-toggle-maximize",
    "core:window:allow-hide"
  ],
  "windows": ["*"]
}
